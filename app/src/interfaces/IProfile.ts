export default interface IProfile{
  username: string,
  phoneNumber: string,
  email: string,
  decency: number, 
  reports: number,
  subExpiresIn: string
  photo: string
  date_joined: string
}
